<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xxx.betwars">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EnglandActivity"
            android:label="ΑΓΓΛΙΑ" />
        <activity android:name=".ItalyActivity"
            android:label="ΙΤΑΛΙΑ" />
        <activity android:name=".GermanyActivity"
            android:label="ΓΕΡΜΑΝΙΑ" />
        <activity android:name=".SpainActivity"
            android:label="ΙΣΠΑΝΙΑ" />
        <activity android:name=".FranceActivity"
            android:label="ΓΑΛΛΙΑ" >
        </activity>
    </application>

</manifest>